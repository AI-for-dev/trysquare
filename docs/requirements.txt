# Documentation build only. These are never imported by trysquare/, and mirror the
# `docs` optional group in pyproject.toml.
sphinx>=8.1
myst-parser[linkify]>=4.0
sphinx-design>=0.6
furo>=2024.8.6
sphinx-copybutton>=0.5.2
