---
myst:
  html_meta:
    "description": "Every trysquare command, every flag, and what it costs - on one page."
---

# Cheat sheet

<!-- The sheet is a poster rather than prose: a card grid, a cost marker per command, and
     the brand's slate and brass, styled by `_static/cheatsheet.css`. It is raw HTML
     because a layout meant to be scanned is not something Markdown expresses, and it
     keeps the theme's sidebar because navigation is the theme's job.

     `cli.md` remains the page that explains *why* each refusal exists; nothing here is a
     second copy of that reasoning. `tests/test_cheatsheet.py` compares the sheet's flags
     against the parser in both directions, so it cannot go quietly out of date. -->

```{raw} html
:file: cheatsheet-body.html
```
