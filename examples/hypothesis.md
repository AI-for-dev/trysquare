# Hypothesis

The `rule` cell touches fewer files outside the scope of the fix than the
baseline: the project rule names what must not move, so `in_scope` rises without
`tests` falling. Nothing would look like: the same `in_scope` rate in both
cells, the gap inconclusive at n=10.

Written before measuring - a hypothesis recorded after the numbers exist is a
caption, not a prediction.
