__pycache__/
*.pyc
.pytest_cache/
.venv/
dist/
build/
*.egg-info/

# Measurement output lands wherever --output points. Nothing forces it into the
# repository, and nothing should be committed by accident.
out/
runs/

# Built documentation
docs/_build/

# Local virtualenv (docs build only; the tool needs no dependencies)
.venv/
